# ASL
This project implements a Convolutional Neural Network (CNN) to classify American Sign Language (ASL) hand gestures from images. The model is trained to recognize alphabet signs and predict the corresponding letter from input images.
